## Cài đặt thư viện
API Chat Google
```bash
pip install -q -U google-genai
```
Tự động hóa trình duyệt
```bash
pip install playwright
playwright install
```
Đọc hình ảnh
```bash
pip install Pillow
```
Kiểm tra cửa sổ app Win
```bash
pip install pywin32 psutil
```
## Cần bổ sung
- Thu nhỏ Capture
- Cache

